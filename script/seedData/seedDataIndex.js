const users = require('./userSeedData')
const items = require('./itemSeedData')
const itemPhotos = require('./itemPhotoSeedData')
const channels = require('./channelsSeedData')
const messages = require('./messagesSeedData')

module.exports = {
  users,
  items,
  itemPhotos,
  channels,
  messages,
}
